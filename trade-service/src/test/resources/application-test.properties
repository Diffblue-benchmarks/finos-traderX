# Test configuration to avoid external dependencies
spring.threads.virtual.enabled=false

# Test URLs
people.service.url=http://localhost:18089
account.service.url=http://localhost:18088
reference.data.service.url=http://localhost:18085
trade.feed.address=http://localhost:18086

# Logging for tests
logging.level.root=WARN
